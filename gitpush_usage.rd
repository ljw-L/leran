#注意事项
 1. 用ssh拷贝git库

#基本操作
 1. git clone [ssh adress]
 2. cd [file]
 3. git checkout [branch name] // 进入分支
 4. git pull origin [branch name] // 拉取最新代码的到分支
 5. git add [filename] // 将指定的文件传至暂存区
 6. git commit -m "备注的信息" // 将暂存区的文件保存至本地仓库
 7. git push -u origin [branch name] // 将本地仓库的文件上传到远程仓库
 8. push origin [branch name] // 上传所选中的分支

